steps:
- task: ComponentGovernanceComponentDetection@0
  displayName: Component Detection
  inputs:
    scanType: "Register"
    verbosity: "Verbose"
    alertWarningLevel: "High"
    failOnAlert: true
  condition: or(ne(variables['System.PullRequest.IsFork'], 'True'), ne(variables['IsFork'], 'True'))
