parameters:
  - name: skip
    default: ''

steps:
- ${{ if eq(parameters.skip, 'True') }}:
  - script: echo ${{parameters.skip}}
- ${{ if ne(parameters.skip, 'True') }}:
  - script: echo not ${{parameters.skip}}
# - ${{ if eq(parameters.skip, 'True') }}:
#   - task: ComponentGovernanceComponentDetection@0
#     displayName: Component Detection
#     inputs:
#       scanType: "Register"
#       verbosity: "Verbose"
#       alertWarningLevel: "High"
#       failOnAlert: true
